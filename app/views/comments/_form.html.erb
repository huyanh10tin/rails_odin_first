<%= form_for [@article, @comment] do |f| %>
	<p>
		<%= f.label :author %>
		<%= f.text_field :author %>
	</p>
	<p>
		<%= f.label :body %>
		<%= f.text_area :body %>
	</p>
	<%= f.submit "comment" %>
<% end %>